For all designs I ask you to make, have them be beautiful, not cookie cutter. Make webpages that are fully featured and worthy for production.

By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, etc unless absolutely necessary or I request them.

Use icons from lucide-react for logos.
---
# PROJECT CONTEXT & STRICT RULES

**CRITICAL:** You must follow these 8 rules for every code generation.

RULE 0: GITHUB REPO IS "freelancescreatifs/Belleya.app"
- The repo was transferred to the personal account to allow free edits.
- **Target Repo:** `freelancescreatifs/Belleya.app`
- **Branch:** `main`
- **Action:** After every successful code change, run `git push origin main` or use the Source Control UI to sync.

RULE 1: DATABASE -- INTERNAL BOLT DB ONLY
- **Active URL:** `https://lldznuayrxzvliehywoc.supabase.co`
- This is the fully populated internal database (950+ clients).
- **IGNORE** any other Supabase URLs found in old docs.
- **NEVER** change `VITE_SUPABASE_URL` in `.env`.

RULE 2: NO "supabase.auth.admin" IN FRONTEND
- **FORBIDDEN:** `supabase.auth.admin.getUserById()`, `listUsers()`, `deleteUser()`.
- **REASON:** These require the Service Role Key and will fail with 403 Forbidden in the browser.
- **SOLUTION:** To get user data, query `supabase.from('admin_users_view')` or `user_profiles`. To delete, use `supabase.rpc('admin_delete_user')`.

RULE 3: TOAST NOTIFICATIONS FIX
- The hook `useToast()` exports `{ showToast }`, NOT `addToast`.
- **Correct Usage:** `showToast('success', 'Message');` (Type first, then message).
- **Fix:** If you see `addToast` in the code, replace it immediately.

RULE 4: PRESERVE ADMIN UI
- When editing `Admin.tsx`, **DO NOT** delete the "Add User" button, the inline "Subscription" dropdowns, or the red "Trash" icon.
- These features were manually added and must be preserved during updates.

RULE 5: KNOWN BUGS TO AUTO-FIX
- If you see `auth.admin.getUserById` in `Admin.tsx` or `RewardsValidation.tsx`, replace it with a database query immediately.
- If you see `addToast`, change it to `showToast`.

RULE 6: COLOR PALETTE
- **NO PURPLE/INDIGO:** Do not use purple or indigo colors unless explicitly requested. Stick to the existing palette.

RULE 7: ENVIRONMENT
- `.env` contains the live production keys. Do not overwrite them with example keys.

RULE 8: EXECUTION
- When I ask for a task, implementing the logic is not enough. You must also check for and fix the Known Bugs (Rule 5) in the file you are touching.